# Cli Commands
here you can see all command listed:

```
ovax scan	<path>	Fast directory walker to find all MP4/MKV files.
ovax hash	<file>	Generates a unique "Head-Tail" SHA256 fingerprint.
ovax forge	<file>	Extracts thumbnails and generates sprite sheets.
ovax info
```