{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build Rust",
			"type": "shell",
			"windows": {
				"command": "cargo build --release; if ($?) { Copy-Item -Path target/release/ovax.exe -Destination ../build/ovax.exe -Force; Copy-Item -Path ffmpeg-lib/bin/*.dll -Destination ../build/ -Force; }"
			},
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"problemMatcher": [],
			"group": "build",
			"presentation": {
				"echo": true,
				"reveal": "always",
				"panel": "shared"
			}
		},
	]
}
