{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Rust",
            "type": "shell",
			"windows": {
				"command": "cargo build --release; if ($?) { if (!(Test-Path build)) { New-Item -ItemType Directory -Path build | Out-Null }; Copy-Item -Path target/release/ovax.exe -Destination build/ovax.exe -Force; Copy-Item -Path ffmpeg-lib/bin/*.dll -Destination build/ -Force; Write-Host \"✅ Build complete: Files copied to ./build/\" -ForegroundColor Green }"
			},
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "build",
            "presentation": {
                "echo": true,
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}